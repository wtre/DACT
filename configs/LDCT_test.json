{
    # testing settings
    "batch_size": 2,
    "print_freq": 100,
    "num_workers": 4, 
    "gpu_id": 1,
    # path to the binary file
    "model_path": "./models_DANet/model_019_ssim_8016",

    # additional settings (not yet)

    ############### network architecture ###############
    # number of filters of the first convolution in UNet
    "wf": 32,   
    # depth of UNet
    "depth": 5, 
    # number of filters of the first convolution in Discriminator
    "ndf": 64,  

    ######### training and validation data path ########
    "SIDD_train_h5": "../../datasets/sidd/small_imgs_train.hdf5", 
    "SIDD_test_h5": "../../datasets/sidd/small_imgs_test.hdf5", 

    # saving models and logs
    "log_dir": "./tests_DANet"
}
